# Reality proxy

The simulator represents a synthetic reality proxy.

The proxy may contain latent states such as:

- household profile,
- person behaviour,
- device state,
- travel intent,
- shower intent,
- actual weather outcome,
- device failure state.

These states generate synthetic reality. They must not be exposed directly to ML or bidding models.
